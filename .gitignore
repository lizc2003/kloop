# Rust build artifacts
rust/target/

# agent runtime offload output
.kloop/
rust/.kloop/

# reference material (local copies of other repos, not ours to version) —
# keep only our own guide. The Claude Code parity corpus lives here too and is
# deliberately NOT versioned: its captures embed that product's own system
# prompt and tool definitions, which are not ours to redistribute.
refs/*
!refs/README.md

# Python fixture tooling
__pycache__/
*.pyc

# Claude Code local state
.claude/
