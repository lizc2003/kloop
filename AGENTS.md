# kloop — 全局记忆

从零做一个新的 Rust agent。详细交接(当前状态、能力清单、实现教训、进度)读 `docs/plan/HANDOFF.md`。

- **kloop 是主开发对象**。参考项目都在 `refs/` 下、只读:`refs/codex`(上游 openai/codex,引擎 codex-rs)、`refs/claude-code`(TS 版)、`refs/codewhale`,仅供参考,绝不在其上开发或推送(`refs/*` 全部被根 `.gitignore` 排除,包括 `refs/claude-code-2.1.220/` 这份 parity 语料——它的 capture 里嵌着对照产品自己的 system prompt 与工具定义,不是我们能再分发的东西,只留在生成它的那台机器上);`refs/claw-code` 已退休为固定 commit 的历史调研来源,结论见 `refs/README.md`,不要假设本地克隆仍存在。kloop 自身开发直接在 main 分支,不开 feature 分支。
- **工作方式**:`docs/plan/` 一个编号文件 = 一个会话任务(✅ 为已完成历史)。开工前读 `docs/plan/HANDOFF.md` + 对应 plan 文件,plan 里标"开工时定/问用户"的点先问清。
- **完成标准(只适用于本次实际改了仓库文件的任务)**:cargo fmt + clippy + test 全绿,一次 commit(写清验证方式);行为变更同步 `rust/DESIGN.md`——**先读那一段现在还成不成立,再决定改写还是追加**(它 3800 行、累计 +4086/−266,只追加正是过期的来路);有 plan 的任务在 plan 文件补 ✅ 与提交号;新教训写进 HANDOFF.md。**只读任务**(审查、调研、解释代码、回答问题)不适用:不检出 worktree、不编译、不跑测试,除非用户明确要求验证;结论的证据来自读代码,不要用跑一遍全量测试来代替判断。
- **提交信息用英文**(2026-09-22 定,仓库已公开在 GitHub 上):commit message 的标题与正文全用英文,验证方式也写英文。有 plan 的任务照旧写 `type(planNNN): ...`,**简单提交(文档、小修、零碎杂活)不必为它造一个 plan 文件**,scope 省掉直接写 `docs: ...`/`fix: ...`。历史提交不回改。其余照旧用中文——与用户交流、`docs/`、plan 文件、代码注释、DESIGN.md。
- **验证**:仓库根有 Makefile,**本地这一道就是全部门禁**(GitHub Actions 已于 2026-09-22 去掉,用户判「目前不需要 CI」)——`make test`(= `cd rust && cargo test`,1 秒,无网络无 key)、`make check`(fmt + clippy + test,提交前跑这一条)、`make mock`。真实 API 的代理和 key 问用户要,勿写进任何提交文件。
- **引用外部代码只写结论,不写坐标**(2026-09-21 定,之前为公开清过一次):私有/闭源项目的文件路径与行号、符号名、二进制指纹(SHA、大小、byte 偏移)、内部服务名及其实测数字(延迟、用量)、对方的提示词与工具描述原文、以及运行捕获,**一律不进仓库**——docs、代码注释、提交信息同等适用。判据是**读者能不能自己打开它**:公开 OSS(`refs/codex` 等 Apache/MIT)照常写 `file:line`,因为任何人都能去核;打不开的一律换成中性描述("被改的那个适配器"、"领域层的契约"),**论证靠形状不靠坐标**——一条脱离那个仓库就没有意义的引用,本来也不是论据。个人路径写 `~` 或 `<repo>`,不写 `/Users/<name>`。
- **风格**:format! 内联变量;match 尽量穷尽;避免 bool 位置参数(必要时 /*param*/ 注释);测试整对象断言优先;新增行为必须带测试;注释只写代码看不出来的约束。
- **用户偏好**:中文交流,短句增量澄清,别用多选项问卷,一次问一个点。
