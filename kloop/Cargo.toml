[workspace]
resolver = "2"
members = ["crates/protocol", "crates/provider", "crates/core", "crates/cli"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
kloop-core = { path = "crates/core" }
kloop-protocol = { path = "crates/protocol" }
kloop-provider = { path = "crates/provider" }

anyhow = "1"
futures = "0.3"
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "io-std", "io-util", "process", "signal", "fs", "time", "sync"] }
tokio-util = "0.7"

# test-only
wiremock = "0.6"
